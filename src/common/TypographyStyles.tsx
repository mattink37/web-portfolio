import styled from "@emotion/styled";
import { Typography } from "@mui/material";

export const GrayTypography = styled(Typography)({
  color: "rgb(142, 146, 151)",
});

export const GraySubTextTypography = styled(Typography)({
  color: "rgb(142, 146, 151)",
  fontSize: 'small'
});

export const WhiteTypography = styled(Typography)({
  color: "white",
});

export const DateTypography = styled(Typography)({
  color: "#72767d",
  fontSize: "small",
});